const PlayButton = () => {
    return (
        <View>
            <Button title={"Choisir une carte"} />
        </View>
    )
}

const styles = StyleSheet.create ({
    container: {
        flex: 1,
        justifyContent: 'center',
    }, 
    buttonContainer: {
        margin: 20,
        borderRadius: 10,
        backgro
    }
});

export default PlayButton;